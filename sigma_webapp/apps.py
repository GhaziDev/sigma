from django.apps import AppConfig


class HomepageConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sigma_webapp' #not sure why is this called homepage, but it is supposed to be app name
